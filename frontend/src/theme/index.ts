import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: { main: "#fff" },
    secondary: { main: "#2a2b32" },
    background: { default: "#343541" },
  },
});

export default theme;
